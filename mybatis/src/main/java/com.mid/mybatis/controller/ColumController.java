//package com.mid.project.controller;

/*import com.jsy.service.common.aspect.AutoTracer;
import io.swagger.annotations.Api;
import io.swagger.annotations.ApiOperation;
import lombok.extern.slf4j.Slf4j;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

@Slf4j
@Api(tags = "注解切面测试")
@RestController("/aspect")
public class ColumController {

    @ApiOperation(value = "注解切面测试")
    @AutoTracer(spanName = "myname")
    @GetMapping("/testAspect")
    public String testAspect(){
        log.info("执行业务方法调用");
        return "调用成功";
    }



}*/
